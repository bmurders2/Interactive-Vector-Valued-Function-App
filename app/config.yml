# Dash app config params
dash_config:    
    routes_pathname_prefix: /
    # if directly fronting with a reverse-proxy, add path below
    requests_pathname_prefix: /
    threads: 10
    port: 8080
    host: "0.0.0.0" # '127.0.0.1' for DEV, '0.0.0.0' for PRD
db_config:
    user: 'db_user'
    password: 'db_password'
    port: 'db_port'
    host: 'db_host'